# 默写助手 (Dictation Assistant)

一个面向中小学学生的**语文 + 英语默写练习工具**。纯前端单文件网页应用，打开即用，支持汉字词语、英语单词、英语句子与课文默写，内置语音朗读、手写板、自动批改、错题订正与抄写练习。

> 当前版本：`1.0.24` ｜ 许可证：ISC ｜ 形态：单文件网页应用（PWA，可打包 Android）

## ✨ 功能特性

### 默写模式
- 📝 **普通默写** — 按顺序显示词语/句子，写完逐题核对
- 🔁 **错题订正** — 自动收集错题，一键生成订正练习
- ✍️ **抄写练习** — 每字 3 格描红，帮助规范书写

### 核心能力
- 🔊 **语音朗读** — 基于浏览器原生 Web Speech API，中文 `zh-CN` / 英文 `en-US` 自动切换（英文句子 TTS 效果好）
- 🖊️ **手写板** — 默写后在手写板书写答案，支持压感笔，答案以图片存档
- ✅ **自动批改（OCR）** — 接入 Tesseract.js 对手写答案做 OCR 识别并自动判分（英文可用，**中文手写识别率偏低，建议人工复核**）
- 📚 **集合管理** — 按课程/单元分组（`三年级/上册/第1课` 这种 `/` 层级），默写前可多选集合、父集全选子集
- 📤📥 **词库导入导出** — 词语数据支持 JSON 导入/导出，自动去重，方便备份与跨设备迁移
- 💾 **本地持久化** — 词语本与练习历史存于浏览器 `localStorage`，无需联网/账号

### 支持的题型
| 类型 | 说明 | 语音 | OCR 批改 |
|---|---|---|---|
| 语文词语（cn） | 汉字词语，默写后手写 | 朗读拼音/词语 | 中文手写识别差，建议人工 |
| 英语单词（en） | 英文单词，默写后手写 | ✅ 英文 TTS | ✅ 较可用 |
| 英语句子（en-sentence） | 英文句子，整句一个格子 | ✅ 英文 TTS | ✅ 较可用 |
| 课文默写（textbook） | 长文本段落默写 | 无（中文无音频） | 建议人工 |

## 🚀 使用方法

### 方式一：浏览器直接使用（最简单）
直接用浏览器打开 `index.html` 即可使用。
- 语音朗读需要浏览器支持 Web Speech API（Chrome / Edge 桌面端和安卓端支持最好），且**首次使用需联网**加载语音。
- OCR 批改需要联网从 CDN 加载 Tesseract.js 模型。

### 方式二：打包为 Android APK
```bash
# 1. 安装依赖
npm install

# 2. 添加 Android 平台（首次执行，会生成 android/ 目录）
npx cap add android

# 3. 同步静态资源到 Android
npx cap sync android

# 4. 构建 APK（需要 Android SDK + JDK 21）
cd android
.\gradlew.bat assembleDebug
# APK 输出：android/app/build/outputs/apk/debug/app-debug.apk
```
构建环境要求：JDK 21（Capacitor 8.x）、Android SDK Platform 35 + Build Tools 35.0.0、Gradle 8.14.3+。

CI 自动构建见 `.github/workflows/build-apk.yml`（推送后自动产出 APK）。

## 📦 项目结构

```
dictation-assistant/
├── index.html            # 主应用（单文件，内联 CSS/JS）
├── manifest.json         # PWA Manifest
├── sw.js                 # Service Worker（PWA 离线缓存）
├── icon.png              # 应用图标
├── capacitor.config.json # Capacitor 配置（appId / appName / webDir）
├── package.json          # npm 依赖与脚本
├── .github/workflows/    # GitHub Actions：APK 自动构建
└── README.md
```
> `android/` 与 `www/` 由 Capacitor 命令生成，不在版本库中。

## 🛠️ 技术栈
- **前端** — 纯 HTML + CSS + JavaScript（无框架依赖）
- **语音合成** — Web Speech API（浏览器原生 TTS）
- **手写识别/批改** — Tesseract.js（CDN 加载，中文手写识别率偏低）
- **跨平台** — Capacitor 8.x（Android 打包）+ PWA（Service Worker）
- **持久化** — localStorage

## 📝 数据格式

词语数据（存于 `localStorage`，键 `dictation_words`）：
```json
{
  "word": "苹果",
  "pinyin": "píng guǒ",
  "collection": "三年级/上册/第1课",
  "type": "cn"
}
```
- `word` — 词语/句子原文
- `pinyin` — 拼音（中文词语用，英文可为空）
- `collection` — 所属集合（支持 `/` 层级分隔）
- `type` — 题型：`cn`(语文词语) / `en`(英语单词) / `en-sentence`(英语句子) / `textbook`(课文默写)

## 📤📥 词库导入导出
- **导出**：词语本页面「📤 导出数据」→ 下载 `默写助手_词库_YYYY-MM-DD.json`
- **导入**：「📥 导入数据」→ 选 JSON 文件，相同词语+集合自动去重

## ⚠️ 已知限制
- **中文手写 OCR 识别率偏低**：自动批改对中文不可靠，默认走人工判分；英文单词/句子相对可用。
- **纯前端、无后端**：数据仅存于当前浏览器 `localStorage`，换设备/清缓存会丢失，需手动导出备份。
- **联网依赖**：语音朗读与 OCR 批改首次使用需联网（CDN 资源）。
- 单人维护，仓库尚在持续打磨中。

## 📌 更新日志 (Changelog)

### v1.0.25（2026-09-11）
- **修复「默写错题重复一次」并移除旧错题集。**
  - 根因：历史中间构建写入的错题副本若缺失 `_key`，`copyToWrongCollection` 仅靠 `_key` 去重会再建一条，导致 `words` 中存在重复 `collection='错题'` 条目；而 `getWrongItems()` 不过滤，默写错题会话里同一词出现两次。
  - 修复：`getWrongItems()` 按 `word|from|type` 去重（保留首条）；`copyToWrongCollection` 对缺 `_key` 的历史错题按 `word|from|type` 兜底匹配，杜绝新增重复；选"全部"集合默写时排除「错题」派生集合，避免与原始集合里的同一词重复。
  - 移除旧版独立 `dictation_wrong_book` 的迁移代码（自 v1.0.25 起不再支持旧格式错题数据）。
  - 验证：新增 `_github_review/test_v1025.js`（jsdom 全 PASS：重复错题副本去重、缺 _key 不重复写入、选"全部"不含错题副本）。

### v1.0.24（2026-09-11）
- **错题集重构为「错题」集合（彻底解决"错题集为空"）。** 按用户要求，错题不再存于独立数组，而是作为 `words` 里 `collection='错题'` 的词语，与正常集合同等地位：
  - 答错/跳过的题：自动复制进「错题」集合，并**保留原集合路径到 `from` 字段**（如 `三年级/上册/第1课`），错题集页面与结果页均显示"来源：…"，可知错题归属。
  - 默写「错题」集合时：答对自动**移出**（已掌握），答错/跳过**保留**并错误次数 +1。手写的错题仍需在结果页点「✗ 判错」才进（保持人工批改，不依赖联网 OCR）。
  - 「错题」集合会出现在左侧集合树里，可像普通集合一样直接选中"开始默写"；旧版独立的 `dictation_wrong_book` 数据在首次打开时自动迁移并入。
  - 验证：新增 `_github_review/test_v1024.js`（jsdom 全 PASS：答错复制进错题集合且 from 保留、去重不重复、默写错题答对移出 / 答错保留、手写判错才进、清空移除全部错题条目）；`test_v1023.js`/`test_en_hw.js`/`test_v1020.js`/`test_result_judge.js`/`test_en.js`/`test_wb_null.js`/`test_clear.js`/`test_syntax.js` 回归 PASS。

### v1.0.23（2026-09-11）
- **取消手写 OCR 与自动批改，回到纯人工批改。** 用户反馈 v1.0.22 手写提交后一直"识别中"（Tesseract.js 在离线/弱网环境卡住加载 worker）。`hwSubmit()` 删除对 `autoGradeHw()` 的调用与"识别中…"按钮状态，手写题提交后 `correct=null` 走结果页人工点「✓ 判对 / ✗ 判错」；`finishDictation()` 仅同步 `correct !== null` 的题。保留 `autoGradeHw`/`preprocessHwImage` 为死代码备用。

### v1.0.22（2026-09-11）
- **撤销 v1.0.21「手写题默认判错」——修复「提交后全部判错」的误伤。** v1.0.21 为让手写题进错题集，把 `correct === null` 的题在 `finishDictation()` 里一律判为错，但英文单词手写提交后 `correct` 本就是 `null`，导致**所有英文手写题被一刀切判错**。
  - 根因：应用早有 `autoGradeHw()`（Tesseract OCR，英文走 `eng` 语言、注释标注"英文可用"），但 `hwSubmit()` 从未调用它，手写题始终 `correct=null`，被 v1.0.21 的默认值强制判错。
  - 修复：`hwSubmit()` 对英文/英语句子手写题提交时**真正调用 OCR 自动判分**——高置信度（`matchScore >= 0.85`）自动判对（不进错题集），低置信度/识别失败保持 `null` 留"待批改"，结果页由用户点「✓ 判对 / ✗ 判错」确认；**绝不自动判错任何手写题**，写对的不再被误伤。中文手写 OCR 不准，仍走人工批改。
  - 提交按钮加"识别中…"状态（首题 OCR 加载 worker 稍慢属正常）。
  - 结果页对 `correct === null` 的题显示「? 待批改」+ OCR 识别文本与分值，便于人工核对。
  - 验证：新增 `_github_review/test_v1022.js`（jsdom：英文手写高置信度自动判对不进错题集、低置信度留待批改、中文手写不自动判错、文字题判分不受影响）ALL PASS；`test_syntax.js` 及 v1.0.21 全量回归 PASS。
  - 注意：OCR 需联网（Tesseract.js CDN）；离线时英文手写退化为"待批改"人工判，不会误判。

### v1.0.21（2026-09-10）
- **修复「手写/未批改题漏进错题集」——结果页未批改题默认判错。** 从截图复现发现：用户用英文单词手写板作答后，结果页显示红 ✗，但错题集仍空。根因是手写题提交后 `correct=null`，`finishDictation()` 原设计「只自动同步 `correct !== null` 的题」，手写题必须逐题点「判错」才进；且判错按钮点了之后没有视觉反馈（CSS 缺少 `.btn-danger-outline`），用户容易漏点或以为没生效。
  - `finishDictation()` 在渲染结果页前，先把所有 `correct === null` 且未跳过的题默认判为 `false`，随后统一同步到错题集；答对的文字题仍会自动移出，不会被误加。
  - 结果页继续保留「✓ 判对 / ✗ 判错」按钮，用户可事后把真正写对的题点「判对」移出错题集；重复点同一判定不再重复计数（`manualMark()` 加 `oldCorrect` 判断）。
  - 新增 `.btn-danger-outline` 样式，点「判错」后按钮变红，反馈明确；手写题结果页文案从「你的答案：（无）」改为「你的答案：见手写图」。
  - 验证：`_github_review/test_en_hw.js`（复现截图场景，未点判错即自动进 2 条）ALL PASS；`test_v1020.js`/`test_result_judge.js` 更新断言后全 PASS；`test_wb_null.js`/`test_clear.js`/`test_v1015.js`/`test_v1017.js`/`test_en.js`/`test_syntax.js` 回归 PASS。

### v1.0.20（2026-09-10）
- **修复「批改后错题集仍为空」——结果页批改按钮对所有题型可见。** 之前结果页的「✓ 判对 / ✗ 判错」按钮只在手写/课文题（`correct === null`）时显示，文字题自动判分后不显示按钮，用户误以为"无法批改"；且旧版缓存下手写题的批改写入路径也未生效。现改为**结果页对每一题都显示判对/判错按钮**：
  - 任意题点「✗ 判错」即调用 `syncWrongBookItem()` 立即加入错题集（答对的题点判错也会强制加入）；点「✓ 判对」则从错题集移出。每次判定经 `saveData()` 持久化到 `localStorage`，打开错题集页面即时渲染。
  - 文字题/跳过题仍按原逻辑在 `finishDictation()` 确认提交后自动同步（答对移出、答错/跳过加入），与手动批改互不冲突。
  - 验证：`_github_review/test_v1020.js`（jsdom 端到端真实点击，全 PASS：所有题型按钮显示、文字题原判对点判错强制加入、原判错点判对移出、未判定题点判错加入、localStorage 持久化、错题集页面正确渲染）；`test_result_judge.js`（按钮数断言更新为每题 2 个）同步 PASS。

### v1.0.19（2026-09-10）
- **修复：结果页手动批改后才写入错题集。** 上一版在「检查全部题目」页直接加判定，打断了连续答题流程。现回退到 v1.0.18 的连续答题 + 检查页结构：
  - 文字题、跳过题仍按原逻辑在 `finishDictation()` 确认提交后自动同步错题集（答对移出、答错/跳过加入）。
  - 手写题（`correct === null`）不再被自动加入错题集，而是留在结果页（详细结果）等待用户逐题点「✓ 判对 / ✗ 判错」；每点一次即时调用 `syncWrongBookItem()` 更新错题集并重新渲染结果页。
  - 新增 `renderResultPage()` 专门负责渲染结果页，避免 `manualMark()` 重复写入历史记录。
  - 验证：`_github_review/test_result_judge.js`（jsdom，ALL PASS：结果页判对移出错题、判错加入错题、跳过题自动加入、未批改手写题不提前加入、重复提交不重复计数），v1.0.15/16/17/18 回归套件全部 PASS。

### v1.0.18（2026-09-10）
- **修复：默写错题未进入错题集。** 根因：`finishDictation` 在渲染成绩页与更新错题集前，对 `dictResults` 中的 `null`（未作答题目）直接读 `r.correct` 抛错，导致函数中断、**错题集更新逻辑整段未执行**——只要本次默写存在任意一道未作答的题（例如空着没填、或用「检查/修改」后留空），所有错题都不会写入错题集。
  - 成绩统计 `filter`、成绩页渲染 `map`、错题集更新 `forEach` 三处均加 `null` 防护；未作答的题不再计入成绩、不在成绩页崩溃、也不产生空错题。
  - `manualMark` 同样加 `dictResults[idx]` 非空判断。
  - 回归：新增 `test_wb_null.js` / `test_wb_review_null.js`（含「留空一题仍正确写入错题」端到端），旧套件 v1.0.15/16/17 全部 PASS。

### v1.0.17（2026-09-10）
- ✨ **提交前「检查全部题目」**：所有题目答完后不再直接出结果，而是先进入检查页，逐题列出答案（文本/手写缩略图，未答显「未填写」、跳过显「已跳过」），每题可点「修改」返回重做；底部「确认提交」出结果、「继续答题」返回。答题页新增「检查」按钮，可随时查看全部进度。
- ✨ **橡皮大小可调**：手写板与抄写练习的橡皮擦范围此前固定偏小。现新增橡皮大小滑块（6–48，默认 10，画布内按 2× 换算），开启橡皮时显示、关闭时隐藏；清除时一并复位。手写擦除 `lineWidth = 橡皮大小×2`，抄写擦除同步生效。
- 验证：`_github_review/test_v1017.js`（jsdom，26 项断言 ALL PASS：检查页出现/逐题修改回填/确认提交/中途检查未填写、橡皮控件开关与大小联动）；`test_v1015.js`、`test_clear.js` 回归无破坏。

### v1.0.16（2026-09-09）
- 🐛 **修复「清除所有数据」后默写页残留原数据**：原 `clearAllData()` 只清空 `words`/`history` 并重渲染词库与统计，未重置进行中的默写会话（`dictList`/`dictResults`/`dictIdx`），也未刷新默写页的集合选择器（`updateCollectionList`），且漏清错题集 `wrongBook`。
  - 现 `clearAllData()` 一并清空 `wrongBook` 并 `saveData()`；重置 `dictList`/`dictResults`/`dictIdx`/`dictStartTime`/`hwRestoreImg`，若正停留在默写/练习页则退回默写设置页；重渲染词库、统计、**默写集合列表**、错题集。
  - `switchPage('dictation')` 进入默写页时额外调用 `updateCollectionList()`，确保集合选择器始终按当前 `words` 重新渲染，杜绝任何残留。
  - 验证：`_github_review/test_clear.js`（jsdom，17 项断言 ALL PASS）；`test_v1015.js` 回归无破坏。

### v1.0.15（2026-09-09）
- **默写可返回上一题修改**：默写答题页新增「← 上一题」按钮（仅非第一题时显示）。点击后保存当前进度并返回上一题，已填写的答案/已手写内容自动回填；回到该题修改后「确定」即原地覆盖（不再追加重复结果），可一路前进回到原处。课文手写题返回时，手写板会显示「上次手写」参考图，可参考后重新书写覆盖。
- **批改放大时同步显示正确答案**：结果页/批改页点击手写缩略图全屏放大时，lightbox 内新增「正确答案（供批改参考）」面板，自动显示该题对应的原文（课文按段落原样展示），批改课文无需再来回对照，放大即可一眼比对。

### v1.0.14（2026-09-09）
- **手写板「清除」二次确认**：默写时手写板的「清除」按钮改为二次点击确认——首次点击按钮变红显示「确认清除？」，3 秒内再次点击才真正擦除刚写的内容，超时自动恢复，避免误点丢失手写答案。
- **课文默写手写区按段落分页**：课文默写时手写板不再用单个受限区域，而是按 `\n` 将课文拆成多页（每页一个手写区、高度按段落长度自适应），手写板底部出现「← 上一页 / 下一页 →」翻页控件；提交时所有页纵向合并为一张长图（批改时不再被压扁看不清）。
- **批改手写缩略图点击放大**：结果页与批改页的手写小图均可点击，弹出全屏 lightbox 查看大图，再次点击/关闭按钮退出。
- **新增「错题集」**：底部导航新增「错题集」页。每次正式默写结束后，结果会**自动更新**错题集——答错或跳过的词自动加入（记录错次/时间），答对的词自动移除；错题集页可查看全部错题、单条「练习」（进入手写订正）或「已掌握」（移出），也支持「开始错题练习」整体强化与「清空错题集」。

### v1.0.13（2026-09-09）
- **修复「新建集合」弹窗不在最上层**：新建/编辑集合弹窗 `coll-edit-overlay` 复用 `modal-overlay`（z-index:200），在「📷 拍照添加」的手写板弹窗（z-index:300）里点击「新建集合」时会被手写板遮住。现已给该弹窗独立设置 `z-index:400`，高于所有其它图层（头部/底栏 100、批量条 150、普通弹窗 200、手写板 300），无论在哪一层弹窗中触发都显示在最顶层。

### v1.0.12（2026-09-09）
- **集合管理增强**：① 每个集合标题栏右侧新增「✏️ 重命名」「🗑️ 删除」按钮——删除会连同其全部子集合与词语一起删除（带二次确认）；重命名通过「新建/编辑集合」弹窗改路径前缀，并同步更新该集合及子树下所有词语的归属路径。② 每条词语/课文项新增「✏️ 修改」按钮，弹窗预填原内容（类型、所属集合、词语或课文标题+正文），保存即原地替换；修改时隐藏批量输入框，课文类型自动切换正文编辑区。
- **顺手修了潜在报错**：`deleteCollection`/导入/别处调用了未定义的 `updateStats()`，统一改为已存在的 `renderStats()`，避免控制台抛 ReferenceError。

### v1.0.11（2026-09-09）
- **修复「词语本不显示集合和词语」**：`renderWordList` 内的 `countItems` 递归统计子树词数时写错——只统计子节点的 `.children` 而漏算了每个中间节点自身的直接词语，导致「若某集合分组的词只加在叶子层（如 `三年级/上册/第1课`）」时，中间节点 `三年级`/`上册` 被算成 0 项，`if (totalItems === 0) return;` 直接整组跳过不渲染，词语全部消失。现已改为正确遍历 `n.children` 汇总子树全部词数，多级集合正常显示。

### v1.0.10（2026-09-08）
- **修复「修改集合」编辑多级集合时的路径错乱**：v1.0.9 的 `openCollEdit` 在传入的上级集合（如 `三年级/上册`）未单独存在于下拉项时，会被重置为顶层、导致「完整路径」预览错拼成 `/第1课`。现改为：上级在列表里直接选中；非空但不在列表里则自动切到「＋ 新建上级集合…」并回填该路径到上级输入框；空则为顶层。预览/确认均正确拼出 `三年级/上册/第1课`。

### v1.0.9（2026-09-08）
- **修复「新建集合」找不到集合名输入框**：重排「新建/编辑集合」弹窗——把「名称」输入框提到第一位并显式加边框/聚焦样式、增加非空校验（确定时若未填名称会提示）；「上级集合」改为可选、放第二位。原「＋ 新建上级集合…」不再弹原生 `prompt()`（手机端易丢失焦点），改为在弹窗内就地展开一个上级路径输入框，路径实时拼入「完整路径」预览。添加词语 / 批量移动 / 修改集合三处入口均生效。

### v1.0.8（2026-09-08）
- **修复「新建集合」无法新建子集合**：在「新建/编辑集合」弹窗的「上级集合」下拉框末尾新增「＋ 新建上级集合…」选项。选中后输入新的上级路径（支持 `/` 分隔多级，如 `三年级/上册`），即可一次性创建任意深度的子集合（如 `三年级/上册/第1课`），不再受限于只能选已有集合作父级。

### v1.0.7（2026-09-08）
- **合集创建不再需要手输 `/`**：新增「新建/编辑集合」弹窗，用「上级集合」下拉框选择父级（列出所有现有集合）+「名称」输入叶子名，实时预览完整路径。添加词语、批量移动、修改集合三处入口统一改用此弹窗，知识层级（如 `三年级/上册/第1课`）照常保留，只是不再靠手敲斜杠。

### v1.0.6（2026-09-08）
- **UI 整体美化**：统一设计语言，采用更柔和的圆角、分层阴影与蓝→浅蓝渐变主色；中文字体栈优化（PingFang SC / 微软雅黑）。
- 头部与底部导航改为圆角渐变 + 激活态指示条；按钮改为胶囊形并加渐变与按压反馈。
- 卡片、词语条目、集合分组、统计卡片（四色渐变）、弹窗（毛玻璃背景 + 弹出动效）、空状态、手写/批改面板等全面提升质感。
- 新增页面/弹窗淡入与上浮动效（尊重 `prefers-reduced-motion`），输入框聚焦光环。

### v1.0.5（2026-09-08）
- **课文默写输入框改进**：默写答案框由单行 `input` 改为可自动增高、超长可滚动的 `textarea`；课文默写时左对齐、最高约 440px，长课文不再写不下。
- **批改答案预览放大**：核对页「正确答案」对课文内容采用浅黄底卡片、可滚动（最高 360px）、保留换行，内容多时清晰可读；详细结果列表中的题目与答案也支持换行显示。

### v1.0.4（2026-09-04）
- **集合管理**：移除默认「未分类」集合。空词库不再自动生成该集合；未指定集合的词不再写入占位值「未分类」，仅作为列表页显示分组（`📋 未分类`）承载无集合词。旧数据加载时自动将 `未分类` 还原为空集合。
- **导入修复**：导入词库后同步内存 `words` 数组并即时刷新列表（此前仅写 localStorage，需手动刷新页面才生效）；导入时兼容还原旧「未分类」占位值。

### v1.0.3（2026-09-04）
- 整理为正式项目：重写 README、统一版本号、补全 package.json 元数据。
- 语音朗读优化：按语言分句朗读（中文夹英文不再带中文腔）、优选高质量 voice 并缓存。

### v1.0.2 及更早
- 初始版本：普通默写、错题订正、抄写练习、手写板、OCR 批改、集合层级管理、词库导入导出、PWA 与 Capacitor Android 打包。

## 🗺️ 后续规划
- 内置常用教材词库（如人教版语文、外研社英语），开箱即用
- 复习计划（FSRS / 艾宾浩斯遗忘曲线）自动安排薄弱词
- 离线语音包 / 本地 OCR，断网可用
- 词库云端同步

## 📄 License
ISC

## 🙏 致谢
- [Capacitor](https://capacitorjs.com/) — 跨平台原生运行时
- [Tesseract.js](https://tesseract.projectnaptha.com/) — 手写识别/OCR
- [jsDelivr](https://www.jsdelivr.com/) — 前端 CDN
